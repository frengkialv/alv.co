import { ColorProduct } from "@/types/stock";

interface GetStockQuantity {
  productId: string;
  color: ColorProduct;
}

// export async function getStockQuantity({productId, color, size}: ) {

// }
